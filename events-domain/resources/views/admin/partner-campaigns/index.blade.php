<x-app-layout>
    <x-slot name="header"><h2 class="font-semibold text-xl text-gray-800 leading-tight">{{ __('Partner Campaigns') }}</h2></x-slot>
    <div class="space-y-6"><div class="container-page">
        <div class="card p-4 mb-6"><form method="GET" class="flex flex-wrap gap-4"><select name="status" class="input-field w-auto"><option value="">All Status</option><option value="planning" {{ request('status')==='planning'?'selected':'' }}>Planning</option><option value="active" {{ request('status')==='active'?'selected':'' }}>Active</option><option value="completed" {{ request('status')==='completed'?'selected':'' }}>Completed</option><option value="paused" {{ request('status')==='paused'?'selected':'' }}>Paused</option></select><input type="text" name="search" placeholder="Search..." value="{{ request('search') }}" class="input-field w-64"><button type="submit" class="btn-primary text-sm">Search</button></form></div>
        <div class="card overflow-hidden"><table class="w-full"><thead class="bg-gray-50"><tr><th class="px-6 py-3 text-left text-xs font-medium uppercase">Name</th><th class="px-6 py-3 text-left text-xs font-medium uppercase">Partner</th><th class="px-6 py-3 text-left text-xs font-medium uppercase">Sponsor</th><th class="px-6 py-3 text-left text-xs font-medium uppercase">Event</th><th class="px-6 py-3 text-left text-xs font-medium uppercase">Status</th><th class="px-6 py-3 text-right text-xs font-medium uppercase">Actions</th></tr></thead><tbody class="divide-y">@forelse($campaigns as $c)<tr class="hover:bg-gray-50"><td class="px-6 py-4 text-sm font-medium">{{ $c->name ?? 'N/A' }}</td><td class="px-6 py-4 text-sm text-gray-600">{{ $c->partner->name ?? 'N/A' }}</td><td class="px-6 py-4 text-sm text-gray-600">{{ $c->sponsor->name ?? 'N/A' }}</td><td class="px-6 py-4 text-sm text-gray-600">{{ $c->event->title ?? 'N/A' }}</td><td class="px-6 py-4"><span class="badge badge-{{ $c->status === 'active' ? 'success' : ($c->status === 'completed' ? 'info' : 'warning') }}">{{ ucfirst($c->status) }}</span></td><td class="px-6 py-4 text-right"><a href="{{ route('admin.partner-campaigns.show', $c) }}" class="text-terracotta-500 hover:underline text-sm">View</a></td></tr>@empty <tr><td colspan="6" class="px-6 py-12 text-center text-gray-500">No campaigns found.</td></tr>@endforelse</tbody></table><div class="px-6 py-4 border-t">{{ $campaigns->withQueryString()->links() }}</div></div>
    </div></div>
</x-app-layout>
