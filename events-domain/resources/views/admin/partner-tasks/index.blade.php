<x-app-layout>
    <x-slot name="header"><h2 class="font-semibold text-xl text-gray-800 leading-tight">{{ __('Partner Tasks') }}</h2></x-slot>
    <div class="space-y-6"><div class="container-page">
        <div class="card p-4 mb-6"><form method="GET" class="flex flex-wrap gap-4"><select name="status" class="input-field w-auto"><option value="">All Status</option><option value="pending" {{ request('status')==='pending'?'selected':'' }}>Pending</option><option value="in_progress" {{ request('status')==='in_progress'?'selected':'' }}>In Progress</option><option value="completed" {{ request('status')==='completed'?'selected':'' }}>Completed</option><option value="cancelled" {{ request('status')==='cancelled'?'selected':'' }}>Cancelled</option></select><select name="priority" class="input-field w-auto"><option value="">All Priority</option><option value="low" {{ request('priority')==='low'?'selected':'' }}>Low</option><option value="medium" {{ request('priority')==='medium'?'selected':'' }}>Medium</option><option value="high" {{ request('priority')==='high'?'selected':'' }}>High</option><option value="urgent" {{ request('priority')==='urgent'?'selected':'' }}>Urgent</option></select><input type="text" name="search" placeholder="Search..." value="{{ request('search') }}" class="input-field w-64"><button type="submit" class="btn-primary text-sm">Search</button></form></div>
        <div class="card overflow-hidden"><table class="w-full"><thead class="bg-gray-50"><tr><th class="px-6 py-3 text-left text-xs font-medium uppercase">Title</th><th class="px-6 py-3 text-left text-xs font-medium uppercase">Partner</th><th class="px-6 py-3 text-left text-xs font-medium uppercase">Priority</th><th class="px-6 py-3 text-left text-xs font-medium uppercase">Status</th><th class="px-6 py-3 text-left text-xs font-medium uppercase">Due Date</th><th class="px-6 py-3 text-left text-xs font-medium uppercase">Assigned To</th><th class="px-6 py-3 text-right text-xs font-medium uppercase">Actions</th></tr></thead><tbody class="divide-y">@forelse($tasks as $t)<tr class="hover:bg-gray-50"><td class="px-6 py-4 text-sm font-medium">{{ $t->title }}</td><td class="px-6 py-4 text-sm text-gray-600">{{ $t->partner->name ?? 'N/A' }}</td><td class="px-6 py-4"><span class="badge badge-{{ $t->priority === 'urgent' ? 'danger' : ($t->priority === 'high' ? 'warning' : 'info') }}">{{ ucfirst($t->priority) }}</span></td><td class="px-6 py-4"><span class="badge badge-{{ $t->status === 'completed' ? 'success' : ($t->status === 'cancelled' ? 'danger' : 'info') }}">{{ ucfirst($t->status) }}</span></td><td class="px-6 py-4 text-sm text-gray-500">{{ $t->due_date?->format('M d, Y') ?? 'N/A' }}</td><td class="px-6 py-4 text-sm text-gray-600">{{ $t->assignedTo->name ?? 'N/A' }}</td><td class="px-6 py-4 text-right"><a href="{{ route('admin.partner-tasks.show', $t) }}" class="text-terracotta-500 hover:underline text-sm">View</a></td></tr>@empty <tr><td colspan="7" class="px-6 py-12 text-center text-gray-500">No tasks found.</td></tr>@endforelse</tbody></table><div class="px-6 py-4 border-t">{{ $tasks->withQueryString()->links() }}</div></div>
    </div></div>
</x-app-layout>
