<x-app-layout>
    <x-slot name="header"><h2 class="font-semibold text-xl text-gray-800 leading-tight">{{ __('Partner Client Assignments') }}</h2></x-slot>
    <div class="space-y-6"><div class="container-page">
        <div class="card p-4 mb-6"><form method="GET" class="flex flex-wrap gap-4"><input type="text" name="search" placeholder="Search partner..." value="{{ request('search') }}" class="input-field w-64"><button type="submit" class="btn-primary text-sm">Search</button></form></div>
        <div class="card overflow-hidden"><table class="w-full"><thead class="bg-gray-50"><tr><th class="px-6 py-3 text-left text-xs font-medium uppercase">Partner</th><th class="px-6 py-3 text-left text-xs font-medium uppercase">Sponsor</th><th class="px-6 py-3 text-left text-xs font-medium uppercase">Assigned At</th></tr></thead><tbody class="divide-y">@forelse($assignments as $a)<tr class="hover:bg-gray-50"><td class="px-6 py-4 text-sm font-medium">{{ $a->partner->name ?? 'N/A' }}</td><td class="px-6 py-4 text-sm text-gray-600">{{ $a->sponsor->name ?? 'N/A' }}</td><td class="px-6 py-4 text-sm text-gray-500">{{ $a->created_at->format('M d, Y') }}</td></tr>@empty <tr><td colspan="3" class="px-6 py-12 text-center text-gray-500">No assignments found.</td></tr>@endforelse</tbody></table><div class="px-6 py-4 border-t">{{ $assignments->withQueryString()->links() }}</div></div>
    </div></div>
</x-app-layout>
