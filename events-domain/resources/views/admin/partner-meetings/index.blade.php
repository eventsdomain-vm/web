<x-app-layout>
    <x-slot name="header"><h2 class="font-semibold text-xl text-gray-800 leading-tight">{{ __('Partner Meetings') }}</h2></x-slot>
    <div class="space-y-6"><div class="container-page">
        <div class="card p-4 mb-6"><form method="GET" class="flex flex-wrap gap-4"><select name="status" class="input-field w-auto"><option value="">All Status</option><option value="scheduled" {{ request('status')==='scheduled'?'selected':'' }}>Scheduled</option><option value="confirmed" {{ request('status')==='confirmed'?'selected':'' }}>Confirmed</option><option value="completed" {{ request('status')==='completed'?'selected':'' }}>Completed</option><option value="cancelled" {{ request('status')==='cancelled'?'selected':'' }}>Cancelled</option></select><select name="type" class="input-field w-auto"><option value="">All Type</option><option value="online" {{ request('type')==='online'?'selected':'' }}>Online</option><option value="phone" {{ request('type')==='phone'?'selected':'' }}>Phone</option><option value="in-person" {{ request('type')==='in-person'?'selected':'' }}>In Person</option></select><input type="text" name="search" placeholder="Search..." value="{{ request('search') }}" class="input-field w-64"><button type="submit" class="btn-primary text-sm">Search</button></form></div>
        <div class="card overflow-hidden"><table class="w-full"><thead class="bg-gray-50"><tr><th class="px-6 py-3 text-left text-xs font-medium uppercase">Title</th><th class="px-6 py-3 text-left text-xs font-medium uppercase">Partner</th><th class="px-6 py-3 text-left text-xs font-medium uppercase">Sponsor</th><th class="px-6 py-3 text-left text-xs font-medium uppercase">Type</th><th class="px-6 py-3 text-left text-xs font-medium uppercase">Start</th><th class="px-6 py-3 text-left text-xs font-medium uppercase">Status</th><th class="px-6 py-3 text-right text-xs font-medium uppercase">Actions</th></tr></thead><tbody class="divide-y">@forelse($meetings as $m)<tr class="hover:bg-gray-50"><td class="px-6 py-4 text-sm font-medium">{{ $m->title }}</td><td class="px-6 py-4 text-sm text-gray-600">{{ $m->partner->name ?? 'N/A' }}</td><td class="px-6 py-4 text-sm text-gray-600">{{ $m->sponsor->name ?? 'N/A' }}</td><td class="px-6 py-4 text-sm">{{ ucfirst($m->type) }}</td><td class="px-6 py-4 text-sm text-gray-500">{{ $m->start_time?->format('M d, Y H:i') ?? 'N/A' }}</td><td class="px-6 py-4"><span class="badge badge-{{ $m->status === 'completed' ? 'success' : ($m->status === 'cancelled' ? 'danger' : 'info') }}">{{ ucfirst($m->status) }}</span></td><td class="px-6 py-4 text-right"><a href="{{ route('admin.partner-meetings.show', $m) }}" class="text-terracotta-500 hover:underline text-sm">View</a></td></tr>@empty <tr><td colspan="7" class="px-6 py-12 text-center text-gray-500">No meetings found.</td></tr>@endforelse</tbody></table><div class="px-6 py-4 border-t">{{ $meetings->withQueryString()->links() }}</div></div>
    </div></div>
</x-app-layout>
